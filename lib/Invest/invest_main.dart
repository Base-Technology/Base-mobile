import 'package:flutter/cupertino.dart';

class investMain extends StatelessWidget {
  const investMain({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Invest in crypto'),
    );
  }
}
